name: Deploy
run-name: ${{ gitea.actor }}'s Deployment
on: [push]

jobs:
  Explore-Gitea-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Install ping
        run: sudo apt-get update && sudo apt-get install -y iputils-ping
      - name: Ping server
        run: ping -c 4 10.0.0.40
